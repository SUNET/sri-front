LIST:
1.Add path in navs:
    + TopHeader
    + SideNavNetWork

                     || 2. create Containers:
                     ||     + folder /containers/NAME_ENTITY
                     ||     + file: SearchNAME_ENTITY
                     || 
                     || 3. create Component:
replaced with script ||     + folder /components/NAME_ENTITY
npm run              ||     + file: SearchNAME_ENTITY
create-entity-list   || 
entityBlock          || 4. Create
entityName           ||     containers/entity/EntityList
                     ||     components/entity/EntityList
                     ||     components/entity/EntityRow

eg: npm run create-entity-list network provider

5. Add Reducer/action FilterColumns

6. Add Query and generate files: npm run relay

7. Add Container in Route (Network.js)


DETAILS:
.Create Query:
    + queries/entity/EntityDetailsQuery.js

.Create containers
    + containers/EntityDetails.js

.Create Components
    + EntityDetails.js

Samples:

- To test scripts
rm -rf src/**/testEntity && npm run create-entity-list testBlock testEntity && npm run create-entity-details testBlock testEntity

- To create list entity
npm run create-entity-list network customer

- To create details entity
npm run create-entity-details network customer
